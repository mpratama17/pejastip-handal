# Copy ke .env.local, isi dari dashboard Supabase project ini (Project Settings > API).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Cuma buat script lokal (psql, seed) — jangan dipakai di kode frontend.
DB_PASSWORD=
